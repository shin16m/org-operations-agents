# Personas for agent-creater

このフォルダにはペルソナ定義（JSON/Markdown）を配置します。Copilot Chat で `prompts.md` のペルソナ問診を実行し、出力された JSON と Markdown をここに保存してください。

運用上の注意:
- ペルソナ内にAPIトークン等の機密情報を含めないでください。
- 実際のAsana操作を行う前に、必ずユーザー確認を行うフローにしてください。

例: `asana_buddy.json` / `asana_buddy.md`
